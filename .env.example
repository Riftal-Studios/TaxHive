# TaxHive Environment Configuration Template
#
# Copy this file to .env.local for local development:
#   cp .env.example .env.local
#
# Then update the values with your actual configuration.
#
# Security: NEVER commit .env.local or any .env files to version control

# ============================================================================
# DATABASE
# ============================================================================
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/gsthive_dev?schema=public"

# ============================================================================
# AUTHENTICATION (NextAuth.js)
# ============================================================================
# NEXTAUTH_URL - Leave commented out in development to auto-detect hostname
# This allows accessing via localhost, 192.168.x.x, custom domains, etc.
# For production, set to your production URL: https://taxhive.app
# NEXTAUTH_URL="http://localhost:3000"

# Trust the Host header for dynamic hostname detection (required for development)
# This allows NextAuth to work with any hostname (localhost, IPs, custom domains)
NEXTAUTH_URL_INTERNAL="http://localhost:3000"
AUTH_TRUST_HOST="true"

# Generate a secure secret: openssl rand -base64 32
NEXTAUTH_SECRET="change-this-to-a-random-secret-in-production"

# ============================================================================
# REDIS (Queue/Cache)
# ============================================================================
REDIS_URL="redis://localhost:6379"
REDIS_HOST="localhost"
REDIS_PORT="6379"
REDIS_PASSWORD=""

# ============================================================================
# EMAIL
# ============================================================================
# EMAIL_PROVIDER: "console" for development (logs to console), "ses" for production
EMAIL_PROVIDER="console"
EMAIL_FROM="TaxHive <noreply@taxhive.app>"

# AWS SES Configuration (for production email)
AWS_SES_ACCESS_KEY_ID=""
AWS_SES_SECRET_ACCESS_KEY=""
AWS_SES_REGION="ap-south-1"
AWS_SES_SMTP_USER=""
AWS_SES_SMTP_PASSWORD=""

# ============================================================================
# EXCHANGE RATES
# ============================================================================
# Get free API key from: https://exchangerate-api.com
EXCHANGE_RATE_API_KEY=""
RBI_API_KEY=""

# ============================================================================
# AWS S3 (File Storage - Optional)
# ============================================================================
AWS_REGION=""
AWS_S3_BUCKET=""
AWS_ACCESS_KEY_ID=""
AWS_SECRET_ACCESS_KEY=""

# ============================================================================
# DEVELOPMENT
# ============================================================================
NODE_ENV="development"
# HOSTNAME - Allow access from any network interface (0.0.0.0)
HOSTNAME="0.0.0.0"

# ============================================================================
# OPTIONAL
# ============================================================================
GITHUB_TOKEN=""
CLOUDFLARE_TUNNEL_TOKEN=""

# ============================================================================
# PostgreSQL (for Docker Compose)
# ============================================================================
POSTGRES_USER="postgres"
POSTGRES_PASSWORD="postgres"
POSTGRES_DB="gsthive_dev"
